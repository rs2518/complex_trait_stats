name: cts

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.7
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - tensorflow=2.0.0
  - statsmodels

# Note that Perl is already included on MacOS. Perl will need to be installed for other systems
